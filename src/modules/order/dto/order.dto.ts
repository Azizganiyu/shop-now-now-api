export enum OrderStatus {
  pending = 'pending',
  completed = 'completed',
  running = 'running',
  canceled = 'canceled',
}

export enum ShipmentStatus {
  pending = 'pending',
  processing = 'processing',
  completed = 'completed',
}

export enum OrderTypes {
  onetime = 'onetime',
  recurring = 'running',
}
